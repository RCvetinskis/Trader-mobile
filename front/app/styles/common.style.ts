import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
  alignItemsCenter: {
    flexDirection:"row",
    alignItems: "center",
    justifyContent:"center"
  },
});
