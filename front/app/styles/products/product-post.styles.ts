import { StyleSheet } from "react-native";

export const productPostStyles = StyleSheet.create({
  card: {
    marginTop: 10,
    marginBottom: 50,
  },
  image: {
    height: 200,
    padding:5
  },
});
